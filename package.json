{
  "name": "sahabhagi",
  "private": true,
  "version": "1.0.0",
  "description": "Sahabhagi (सहभागी) — Ward 10 civic hazard reporting platform. Root convenience scripts for the frontend/ and backend/ workspaces.",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "start:backend": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
